# Mealbot Dependencies
# Generated from pyproject.toml for deployment convenience
# Install: pip install -r requirements.txt

# Web framework and ASGI server
fastapi>=0.109.0
uvicorn>=0.27.0

# Data validation and configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database (async PostgreSQL)
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# Authentication (JWT with Auth0)
pyjwt[crypto]>=2.8.0

# HTTP client (for Mailgun and external services)
httpx>=0.26.0
